#include <iostream>
using namespace std;
void data(string colours[], int size)
{
    int fix;
    int count = 0;
    for (int i = 0; i < size - 1; i++)
    {
        fix = i;
        for (int j = fix; j < size; j++)
        {
            if (colours[j])
        }
    }
}
main()
{
    int n;
    cout << "Enter times you want to colour : ";
    cin >> n;
    string colours[n];
    data(colours, n);
}